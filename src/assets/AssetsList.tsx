// AssetsList.tsx
// Displays a list of assets retrieved from the Vault. This component has no local authority.

import React from 'react';

export function AssetsList({ assets }: { assets?: unknown[] }) {
  return null;
}
