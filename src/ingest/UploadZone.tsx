// UploadZone.tsx
// Collects artifacts for ingest and delegates handling to the Vault. No local file processing occurs here.

import React from 'react';

export function UploadZone({ onUpload }: { onUpload?: () => void }) {
  return null;
}
