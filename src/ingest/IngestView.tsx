// IngestView.tsx
// Coordinates the ingest workflow steps (upload, scan, bind) using Vault responses. Contains no client-side authority.

import React from 'react';

export function IngestView() {
  return null;
}
