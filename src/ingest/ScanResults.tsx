// ScanResults.tsx
// Displays scan outcomes returned by the Vault for uploaded assets. Does not perform client-side evaluation.

import React from 'react';

export function ScanResults({ results }: { results?: unknown }) {
  return null;
}
