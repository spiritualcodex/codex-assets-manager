// BindAction.tsx
// Presents bind controls to associate scanned assets with Vault-managed records. Executes no local authority.

import React from 'react';

export function BindAction({ onBind }: { onBind?: () => void }) {
  return null;
}
