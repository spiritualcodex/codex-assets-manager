// ProviderCard.tsx
// Displays a provider entry as supplied by the Vault, without secrets or client-side authority.

import React from 'react';

export function ProviderCard({ provider }: { provider?: unknown }) {
  return null;
}
