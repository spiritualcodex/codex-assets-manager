// ActivityLog.tsx
// Renders an immutable activity stream sourced from Vault audit events. Does not mutate or filter client-side.

import React from 'react';

export function ActivityLog({ entries }: { entries?: unknown[] }) {
  return null;
}
