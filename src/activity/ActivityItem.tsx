// ActivityItem.tsx
// Displays a single activity entry as provided by Vault audit logs. Contains no local mutations or inference.

import React from 'react';

export function ActivityItem({ entry }: { entry?: unknown }) {
  return null;
}
